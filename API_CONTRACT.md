# قرارداد API — سامانه درخواست‌های دانشجویی

> این سند «توافق‌نامه» بین فرانت و بک‌اند است. هر تغییری در endpoint ها یا
> شکل داده‌ها، **اول باید اینجا آپدیت بشه**، بعد کد بک‌اند/فرانت.
>
> Base URL (توسعه): `http://localhost:8000/api`

---

## قوانین کلی

- همه‌ی endpoint های بخش ادمین نیاز به هدر زیر دارن (به‌جز لاگین):
  ```
  Authorization: Bearer <token>
  ```
- در صورت نامعتبر/منقضی بودن توکن، سرور باید کد وضعیت **401** برگردونه (فرانت با گرفتن 401 خودکار کاربر رو به صفحه لاگین می‌فرسته).
- خطاها باید همیشه به این شکل برگردن تا فرانت بتونه پیامش رو نشون بده:
  ```json
  { "message": "متن خطا به فارسی" }
  ```
- تاریخ‌ها به‌صورت رشته‌ی فرمت‌شده فارسی یا ISO برگردن (باید با هم توافق کنیم، پیشنهاد: ISO `2025-11-02T10:30:00Z` و فرانت خودش فرمتش می‌کنه).

---

## ۱. احراز هویت (Auth)

### `POST /admin/login`
**ورودی:**
```json
{ "username": "admin", "password": "123456" }
```
**خروجی موفق (200):**
```json
{
  "token": "jwt-token-here",
  "role": "manager_department",
  "displayName": "مدیر گروه"
}
```
> `role` باید یکی از این دو مقدار باشه: `"manager_department"` (مدیر گروه) یا `"scientific_association"` (انجمن علمی).

**خروجی خطا (401):**
```json
{ "message": "نام کاربری یا رمز عبور اشتباه است" }
```

### `POST /admin/logout`
بدون بدنه، فقط هدر Authorization. خروجی: 200 خالی کافیه.

---

## ۲. درخواست‌های دانشجویی (Requests)

### `POST /requests`
> این endpoint احراز هویت نمی‌خواهد (دانشجو لاگین نمی‌کند، فقط از طریق کد پیگیری بعداً وضعیتش را می‌بیند).

**ورودی:**
```json
{
  "type": "extra",
  "formData": {
    "name": "علی",
    "lastName": "رضایی",
    "studentId": "40123456",
    "courseCode": "12345",
    "courseName": "مبانی کامپیوتر",
    "units": "3",
    "semester": "402",
    "description": "..."
  }
}
```
> `type` یکی از ۱۴ شناسه نوع درخواست است: `extra`, `intergroup`, `capacity`, `general`, `introduce`, `leaveWith`, `leaveWithout`, `olympiad`, `teachingAssistant`, `extension`, `guest`, `project`, `seminar`, `masterProject`.
> `formData` بسته به `type` فیلدهای متفاوتی دارد (هر نوع schema خودش را دارد در فرانت) — بک‌اند باید بر اساس `type` اعتبارسنجی مطابق را روی `formData` انجام دهد.

**خروجی موفق (201):**
```json
{
  "trackingCode": "A3F7K9"
}
```
> ⚠️ **مهم:** کد پیگیری باید حتماً سمت **سرور** تولید بشه (نه فرانت)، چون باید به رکورد واقعی دیتابیس متصل باشه و یکتا بودنش تضمین بشه. فرمت طبق سند دیتابیس: دقیقاً **۶ کاراکتر**، فقط حروف بزرگ انگلیسی و اعداد انگلیسی (مثل `A3F7K9`).

**خطا (400 - داده نامعتبر):**
```json
{ "message": "اطلاعات ارسالی نامعتبر است" }
```

### `GET /requests/track/:trackingCode`
> این هم بدون نیاز به احراز هویت (عمومی، هر کسی با کد پیگیری صحیح می‌تواند ببیند).

**خروجی موفق (200):**
```json
{
  "type": "درخواست ارائه درس اضافه",
  "code": "A3F7K9",
  "status": "تایید شده",
  "name": "علی رضایی",
  "studentId": "40123456",
  "date": "1403/08/15",
  "time": "10:30",
  "description": "توضیحات اضافی در صورت وجود"
}
```

**خطا (404 - کد پیگیری یافت نشد):**
```json
{ "message": "درخواستی با این کد پیگیری یافت نشد" }
```

---

## ۳. درخواست‌های پنل ادمین (Admin Requests)

### `GET /admin/stats`
**خروجی (200):**
```json
{
  "total": 120,
  "pending": 30,
  "approved": 70,
  "rejected": 20
}
```

### `GET /admin/requests`
**Query params:**
| پارامتر | نوع | توضیح |
|---|---|---|
| `status` | string | `pending` \| `approved` \| `rejected` \| خالی (همه) |
| `type` | string | نوع درخواست (کد نوع، پایین لیست شده) \| خالی (همه) |
| `search` | string | جستجو در نام/شماره دانشجویی/کد پیگیری |
| `page` | number | شماره صفحه (پیش‌فرض ۱) |
| `limit` | number | تعداد در هر صفحه (پیش‌فرض ۱۰) |

**خروجی (200):**
```json
{
  "requests": [
    {
      "trackingCode": "REQ-12345",
      "type": "درخواست ارائه درس اضافه",
      "studentName": "علی رضایی",
      "studentId": "401234567",
      "date": "2025-11-02T10:30:00Z",
      "status": "pending"
    }
  ],
  "total": 120,
  "totalPages": 12
}
```

### `GET /admin/requests/:trackingCode`
**خروجی (200):**
```json
{
  "trackingCode": "REQ-12345",
  "type": "درخواست ارائه درس اضافه",
  "studentName": "علی رضایی",
  "studentId": "401234567",
  "date": "2025-11-02T10:30:00Z",
  "status": "pending",
  "formData": {
    "کد درس": "1234567",
    "نام درس": "مبانی کامپیوتر",
    "واحد درس": "3",
    "توضیحات": "..."
  },
  "rejectionReason": null,
  "history": [
    {
      "status": "pending",
      "date": "2025-11-02",
      "time": "10:30",
      "note": null,
      "reason": null
    }
  ]
}
```
> `formData` آبجکتیه که کلیدهاش برچسب فارسی فیلد و مقدارش جواب دانشجوئه — همون فیلدهایی که توی پروپوزال برای هر نوع درخواست تعریف شده (۵ نوع فعلی). چون هر نوع فرم فیلدهای متفاوت داره، بهتره این آبجکت pass-through باشه نه ساختار ثابت.

**خطا (404):**
```json
{ "message": "درخواستی با این کد پیگیری یافت نشد" }
```

### `PATCH /admin/requests/:trackingCode/status`
> ⚠️ فقط نقش `manager_department` (مدیر گروه) مجاز به این عملیاته، نه `scientific_association`. این چک باید توی بک‌اند هم انجام بشه (فرانت فقط UI رو مخفی می‌کنه، امنیت واقعی سمت سرور باید باشه).

**ورودی:**
```json
{
  "status": "approved",
  "rejectionReason": "اختیاری - فقط برای status=rejected",
  "adminNote": "اختیاری"
}
```
**خروجی (200):** همون آبجکت درخواست به‌روزشده (مثل GET بالا) رو برگردون.

### `GET /admin/requests/export/excel`
### `GET /admin/requests/export/pdf`
همون query paramهای بالا (`status`, `type`, `search`) رو می‌گیرن.
**خروجی:** فایل باینری (`Content-Type: application/vnd.ms-excel` یا `application/pdf`) — فرانت با `responseType: 'blob'` می‌گیرتش.

---

## ۴. چت‌بات

### `POST /chatbot/message`
**ورودی:**
```json
{
  "message": "چطور درخواست ارائه درس اضافه بدم؟",
  "history": [
    { "role": "user", "content": "سلام" },
    { "role": "assistant", "content": "سلام، چطور می‌تونم کمک کنم؟" }
  ]
}
```
**خروجی (200):**
```json
{
  "reply": "برای این کار باید...",
  "suggestions": ["ثبت درخواست جدید", "سوالات متداول"],
  "confidence": 0.92
}
```
> `suggestions` و `confidence` اختیاری هستن، ولی `reply` الزامیه.

### `GET /chatbot/suggestions`
**خروجی (200):**
```json
{ "suggestions": ["چطور درخواست ثبت کنم؟", "کد پیگیری رو کجا وارد کنم؟"] }
```

### `POST /chatbot/feedback`
**ورودی:**
```json
{ "messageId": "msg-123", "feedback": "helpful" }
```
> `feedback` یکی از `"helpful"` یا `"not_helpful"`. خروجی: 200 خالی کافیه.

---

## ۵. سوالات متداول (FAQ)

### `GET /faqs`
> بدون نیاز به احراز هویت (عمومی).

**خروجی موفق (200):**
```json
[
  {
    "id": 1,
    "question": "فرآیند ثبت درخواست ارائه درس اضافه چگونه است؟",
    "answer": "دانشجو می‌بایست ابتدا با استاد درس مورد نظر مشورت نموده و..."
  }
]
```
> رنگ و آیکون هر سوال سمت فرانت و به‌صورت چرخشی از یه پالت ثابت تعیین می‌شه، پس بک‌اند فقط `id`, `question`, `answer` رو برمی‌گردونه.

---

## ۶. یادداشت‌های داخلی ادمین (Admin Notes)

> فقط برای نقش‌های ادمین (`manager_department` و `scientific_association`)، هیچ دانشجویی بهش دسترسی نداره. این جایگزین بخش «چت‌بات ادمین» شد که اصلاً هیچ‌وقت پیاده نشده بود.

### `GET /admin/notes`
**خروجی موفق (200):**
```json
[
  {
    "id": 1,
    "title": "هماهنگی امتحانات ترم",
    "content": "متن کامل یادداشت اینجا...",
    "createdAt": "1403/08/15 - 10:30",
    "updatedAt": "1403/08/16 - 14:00"
  }
]
```

### `POST /admin/notes`
**ورودی:**
```json
{ "title": "عنوان یادداشت", "content": "متن یادداشت" }
```
**خروجی (201):** آبجکت یادداشت ساخته‌شده (مثل بالا).

### `PUT /admin/notes/:id`
**ورودی:** همون `{ title, content }`.
**خروجی (200):** آبجکت یادداشت به‌روزشده.

### `DELETE /admin/notes/:id`
**خروجی (200):** خالی کافیه.

---

## نکته برای فاز بعدی (هنوز پیاده نشده سمت فرانت)

طبق پروپوزال و سند دیتابیس، فرم‌های ثبت درخواست فعلاً سمت فرانت هاردکد هستن (۱۴ نوع، هر کدوم با schema و فیلدهای دستی نوشته‌شده در کد). قراره در آینده به سمت «فرم‌ساز داینامیک» بریم — یعنی لیست انواع درخواست و تعریف فیلدهای هر کدوم از یک endpoint بیاد، نه از کد فرانت.

### `GET /request-types`
> این endpoint نیازی به احراز هویت نداره (عمومی، هم دانشجو و هم فرانت موقع رندر فرم استفاده‌ش می‌کنه).

**خروجی موفق (200):**
```json
[
  {
    "id": "guest",
    "title": "مهمان دائم یا موقت",
    "category": "دانشجویی",
    "isActive": true,
    "fields": [
      {
        "name": "name",
        "label": "نام",
        "type": "text",
        "required": true,
        "minLength": 2,
        "placeholder": "نام خود را وارد کنید"
      },
      {
        "name": "studentId",
        "label": "شماره دانشجویی",
        "type": "text",
        "required": true,
        "pattern": "^[0-9]{10}$",
        "placeholder": "مثال: ۹۸۱۲۳۴۵۶"
      },
      {
        "name": "guestType",
        "label": "نوع درخواست",
        "type": "select",
        "required": true,
        "options": [
          { "value": "مهمان دائم", "label": "مهمان دائم" },
          { "value": "مهمان موقت", "label": "مهمان موقت" }
        ]
      },
      {
        "name": "equivalentCourses",
        "label": "دروس معادل‌سازی شده",
        "type": "textarea",
        "required": true,
        "maxLength": 300,
        "fullWidth": true
      }
    ]
  }
]
```

**نکات فرمت (این‌ها را برای فرانت مهم است):**
| کلید | معنی |
|---|---|
| `id` | شناسه یکتای نوع درخواست (انگلیسی، بدون فاصله — همون چیزی که در `POST /requests` به‌عنوان `type` فرستاده میشه) |
| `type` (روی هر فیلد) | یکی از سه مقدار: `text`, `select`, `textarea` (فعلاً فرانت فقط همین سه نوع فیلد رو پشتیبانی می‌کنه) |
| `required` | آیا فیلد اجباریه |
| `minLength` / `maxLength` | محدودیت طول متن (اختیاری) |
| `pattern` | یک regex رشته‌ای برای اعتبارسنجی فرمت (اختیاری، مثل شماره دانشجویی ۱۰ رقمی) |
| `options` | فقط برای `type: "select"` — آرایه‌ای از `{ value, label }` |
| `fullWidth` | آیا فیلد باید کل عرض فرم رو بگیره (مثلاً برای textarea ها) یا نصفش |
| `isActive` | اگه `false` باشه، فرانت اون نوع درخواست رو توی لیست انتخاب نشون نمیده |

⚠️ **این تغییر فعلاً پیاده نشده** — فرانت الان همچنان هاردکده. وقتی هر دو طرف روی این فرمت توافق کردیم، فرانت باید یک تابع `buildZodSchema(fields)` بسازه که این تعریف JSON رو به قوانین اعتبارسنجی واقعی (zod) تبدیل کنه، و `NewRequest.jsx` باید از حالت فعلی (schema های دستی برای هر نوع) به این حالت داینامیک منتقل بشه. این یک تغییر معماری قابل‌توجهه و بهتره قبل از شروعش، هر دو طرف روی این JSON schema بالا توافق نهایی کنن.

طبق پروپوزال، فرم‌های ثبت درخواست جدید (۵ نوع فعلی + موارد آینده مثل مرخصی، پروژه کارشناسی) قراره از یک ساختار «فرم‌ساز» استفاده کنن. این یعنی به‌زودی یه بحث جدا با هم می‌کنیم که آیا:
- **گزینه A:** تعریف فیلدهای فرم فقط سمت فرانت بمونه (سریع‌تر برای فاز اول)، یا
- **گزینه B:** بک‌اند یه endpoint مثل `GET /request-types` برگردونه که تعریف فیلدهای هر فرم رو بده (فرانت کاملاً داینامیک رندر کنه)

فعلاً گزینه A رو در نظر داریم، ولی حتماً قبل از فاز ۱ (ریفکتور فرم‌ها) با بک‌اند هماهنگ می‌کنیم کدوم منطقی‌تره.